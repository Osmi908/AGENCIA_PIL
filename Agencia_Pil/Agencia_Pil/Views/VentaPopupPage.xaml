<?xml version="1.0" encoding="utf-8" ?>
<popup:PopupPage   xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:popup="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             x:Class="Agencia_Pil.Views.VentaPopupPage">
    <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Padding="0" Margin="0">
        <Frame WidthRequest="270" HeightRequest="300" Padding="15">
            <StackLayout>
                <Image Source = "Logo" WidthRequest="70" HeightRequest="80" />
                <Label Text ="NOMBRE:" />
                <Entry Placeholder="{Binding nombre}" />
                <Entry Placeholder="Cantidad" />
                <Button Text="Añadir" BackgroundColor="#1094a1" HeightRequest="35" TextColor="White"></Button>
            </StackLayout>
        </Frame>
    </StackLayout>

</popup:PopupPage>